<!-- Hero Section -->
<section class="relative bg-gradient-to-r from-indigo-600 to-purple-600 text-white py-32">
  <div class="container mx-auto px-5 text-center">
    <h1 class="text-4xl md:text-6xl font-extrabold mb-4">Encontre sua próxima propriedade</h1>
    <p class="text-lg md:text-xl mb-8">Explore nossas casas e apartamentos cuidadosamente selecionados.</p>
    <a href="#properties"
       class="bg-white text-indigo-600 font-bold px-6 py-3 rounded-full shadow hover:shadow-lg transition">
      Ver Propriedades
    </a>
  </div>
</section>

<!-- Grid de Propriedades -->
<section id="properties" class="container mx-auto mt-20 px-5 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
  <% @properties.each do |property| %>
    <%= render "property_card", property: property %>
  <% end %>
</section>
